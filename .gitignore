# Dependencies
node_modules/

# Build output
dist/
build/icon.iconset/
build/icon.icns
build/icon.svg.png

# Native build artifacts (built via build script)
native/coco-speech
native/*.log

# macOS
.DS_Store

# Editor
.vscode/

# Local env / secrets
.env
*.local

# Bridge data (personal — never commit)
*-Bridge/
bridge/
product-backlog.json

# Token / context usage cost monitor — TEMPORARY, dev-only. Not part of the
# shipped solution; kept local so it never commits.
.kiro/usage/
.kiro/hooks/token-usage-monitor.json
scripts/token-usage-monitor.sh
